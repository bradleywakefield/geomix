% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateBeta.R
\name{estimate_beta}
\alias{estimate_beta}
\title{Estimate the Potts interaction parameter beta}
\usage{
estimate_beta(
  geomix_setup,
  niter = 2000,
  nburnin = 250,
  thin = 10,
  nchains = 4
)
}
\arguments{
\item{geomix_setup}{A GeoMix setup object containing the constants, data,
and control settings required to fit the Potts submodel. In particular,
it must contain:
\describe{
\item{\code{controlGibbs}}{A list containing \code{N1}, \code{K}, \code{neighbourID},
\code{neighbourNum}, \code{weights}, and \code{penalty}.}
\item{\code{data_list}}{A list containing the observed class labels \code{Z1}.}
\item{\code{controlHMC}}{A list of control arguments passed to the custom
HMC sampler \code{HMCSample1D_logFD}.}
}}

\item{niter}{Integer giving the total number of MCMC iterations per chain.
Defaults to \code{2000}.}

\item{nburnin}{Integer giving the number of burn-in iterations discarded
from each chain. Defaults to \code{250}.}

\item{thin}{Integer giving the thinning interval used when saving posterior
samples. Defaults to \code{10}.}

\item{nchains}{Integer giving the number of MCMC chains to run.
Defaults to \code{4}.}
}
\value{
A named list with components:
\describe{
\item{estimate}{Numeric scalar giving the posterior mean of the sampled
\code{beta} values across all retained draws.}
\item{samples}{Matrix or vector of retained posterior samples returned by
\code{nimble::runMCMC()}. The exact structure depends on the value of
\code{nchains} and the default return format of \code{runMCMC()}.}
}
}
\description{
Fits a reduced Potts model to the observed class labels \code{Z1} in order to
estimate the spatial interaction parameter \code{beta}. The model is implemented
in \pkg{nimble} and sampled using a custom one-dimensional HMC sampler.
}
\details{
The function constructs a Potts model with \code{Y1} fixed to the observed labels
\code{Z1}, runs MCMC for \code{beta}, and returns both the posterior samples and the
posterior mean estimate.

The fitted model is
\deqn{
\beta \sim \mathrm{Uniform}(0.5, 5),
}
with latent field likelihood
\deqn{
Y_1 \sim \mathrm{Potts}(Z_1, K, \beta, \mathrm{weights},
\mathrm{neighbours}, \mathrm{neighbourNum}, \mathrm{penalty}),
}
where \code{Y1} is initialised at the observed labels \code{Z1}.

The model is compiled with \pkg{nimble}, the default sampler for \code{beta}
is replaced by \code{HMCSample1D_logFD}, and posterior inference is based on
the retained MCMC samples after burn-in and thinning.

This function is typically used to obtain a plug-in estimate of the Potts
interaction parameter for later use in the full GeoMix model.
}
\examples{
\dontrun{
beta_out <- estimate_beta(geomix_setup)

beta_out$estimate
head(beta_out$samples)

beta_out2 <- estimate_beta(
  geomix_setup,
  niter = 5000,
  nburnin = 500,
  thin = 5,
  nchains = 2
)
}

}
\seealso{
\code{\link[nimble:nimbleModel]{nimble::nimbleModel()}}, \code{\link[nimble:runMCMC]{nimble::runMCMC()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{run_mcmc_diagnostics}
\alias{run_mcmc_diagnostics}
\title{Run MCMC diagnostics for GeoMix model outputs}
\usage{
run_mcmc_diagnostics(
  params_list,
  name = "GeoMix",
  exclude = NULL,
  plot_variables = c("alpha0", "alpha1", "sigma2", "lL", "lD", "tau2", "sigma2_L",
    "sigma2_D", "gamma", "h", "logProb"),
  plot_classes = c(1, 2),
  plot_gamma = rbind(c(1, 2), c(1, 3), c(2, 1), c(2, 3)),
  Y1index = NULL
)
}
\arguments{
\item{params_list}{A single fitted GeoMix parameter object with a \verb{$samples}
component, or a list of such objects representing multiple MCMC chains.}

\item{name}{Character string giving the model name used in plot titles.
Defaults to \code{"GeoMix"}.}

\item{exclude}{Optional character vector of parameter names to exclude from
diagnostics after chain matrices are constructed. Defaults to \code{NULL}.}

\item{plot_variables}{Character vector specifying parameter groups to include
in representative diagnostic plots. Defaults to
\code{c("alpha0","alpha1","sigma2","lL","lD","tau2","sigma2_L","sigma2_D","gamma","h","logProb")}.}

\item{plot_classes}{Integer vector of class indices used when selecting
class-specific parameters (such as \code{sigma2[k]}, \code{lL[k]}, \code{lD[k]}) for
plotting. Defaults to \code{c(1,2)}.}

\item{plot_gamma}{Integer matrix with two columns specifying off-diagonal
gamma parameters to include in plots. Each row corresponds to one
\code{gammaMat[i,j]} pair. Defaults to
\code{rbind(c(1,2), c(1,3), c(2,1), c(2,3))}.}

\item{Y1index}{Optional integer vector giving selected lattice indices of
latent labels \code{Y1} to use when producing class-count trace plots.
Defaults to all columns of sampled \code{Y1}.}
}
\value{
A named list containing MCMC draws, diagnostic tables,
summaries, and plots.

\describe{
\item{draws}{Posterior draws in multiple formats.}
\describe{
\item{draws_array}{A \code{posterior::draws_array} object with dimensions
iterations × chains × parameters.}
\item{combined_matrix}{A stacked
\code{posterior::draws_matrix} containing all chains combined by row.}
\item{chain_matrices}{A list of numeric matrices, one per chain,
with rows corresponding to iterations and columns to parameters.}
}

\item{tables}{Diagnostic summary tables.}
\describe{
\item{diagnostics}{Parameter-level diagnostics including \code{rhat},
bulk/tail ESS, posterior SD, MCSE, quantiles, and parameter class.}
\item{class_diagnostics}{Grouped summaries by parameter class
(e.g. \code{sigma2}, \code{lL}, \code{gamma}).}
\item{chain_diagnostics}{Per-chain summaries for each parameter,
including ESS, MCSE, and posterior quantiles.}
\item{overall_diagnostics}{One-row summary of global convergence
statistics across all monitored parameters.}
\item{worst_rhat}{Top 10 parameters ranked by largest \code{rhat}.}
\item{worst_ess}{Top 10 parameters ranked by lowest bulk ESS.}
}

\item{summaries}{General metadata and printed summary text.}
\describe{
\item{key_stats_text}{Character string containing the printed
diagnostic report.}
\item{n_chains}{Number of MCMC chains analysed.}
}

\item{plots}{Diagnostic \code{ggplot2} / \code{bayesplot} objects.}
\describe{
\item{trace_acf}{List containing representative trace plots,
autocorrelation plots, and selected parameter names.}
\item{rank}{Rank histogram overlay plot for selected parameters.}
\item{logProb}{Trace plots of log-posterior components by chain.}
\item{Y1}{Trace plots of sampled latent class counts over iterations.}
}
}
}
\description{
Computes and summarises convergence diagnostics, effective sample sizes,
Monte Carlo standard errors, and representative plots for one or more
MCMC chains produced by GeoMix model fitting routines. The function is
designed for posterior samples stored in \code{params} objects containing a
\verb{$samples} element.
}
\details{
It supports multiple chains, combines draws into \code{posterior} formats,
produces diagnostic tables, rank plots, trace plots, autocorrelation plots,
log-posterior traces, and latent class count summaries.

By default, high-dimensional latent fields such as \code{Y1} are excluded from
standard convergence summaries, but selected summaries of sampled class
counts are provided.

Diagnostics are based on functions from the \pkg{posterior} package,
including:
\itemize{
\item rank-normalised split-\eqn{\hat{R}}
\item bulk effective sample size
\item tail effective sample size
\item Monte Carlo standard error
}

For non-LGFM models, off-diagonal gamma parameters and boundary parameter
\code{h} are automatically included where available.

A printed summary is emitted via \code{message()} when the function runs.
}
\examples{
\dontrun{
# Single chain
out <- run_mcmc_diagnostics(params)

# Multiple chains
out <- run_mcmc_diagnostics(
  params_list = list(chain1, chain2, chain3),
  name = "GeoMix",
  plot_classes = c(1, 4)
)

# View worst mixing parameters
out$tables$worst_rhat

# Print trace plots
out$plots$trace_acf$trace
}

}
\seealso{
\code{\link[posterior:rhat]{posterior::rhat()}}, \code{\link[posterior:ess_bulk]{posterior::ess_bulk()}},
\code{\link[bayesplot:MCMC-traces]{bayesplot::mcmc_trace()}}
}

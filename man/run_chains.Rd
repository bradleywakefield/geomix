% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runChains.R
\name{run_chains}
\alias{run_chains}
\title{Run One or More GeoMix MCMC Chains}
\usage{
run_chains(
  geomix_setup,
  nchains = 1,
  path = NULL,
  controlMCMC = NULL,
  LGFM = FALSE,
  run_parallel = FALSE,
  load_previous_state = FALSE,
  mc.cores = NULL,
  seed = 16
)
}
\arguments{
\item{geomix_setup}{A list containing all model inputs required to build and
run the model. Typically includes elements such as \code{constants},
\code{data_list}, \code{inits}, \code{controlGibbs}, \code{controlHMC}, and optionally
\code{controlMCMC}.}

\item{nchains}{Integer. Number of independent chains to run.
Default is \code{1}.}

\item{path}{Optional character string giving a directory where batch outputs
should be saved when \code{save_batches = TRUE}.
Default is \code{NULL}.}

\item{controlMCMC}{Optional named list of MCMC controls. Values supplied here
override defaults and any values stored in \code{geomix_setup$controlMCMC}.
Supported fields are:
\describe{
\item{niter}{Total number of MCMC iterations per chain.}
\item{thin}{Thinning interval passed to \code{runMCMC()}.}
\item{nbatches}{Number of batches used to split iterations.}
\item{save_batches}{Logical; save each batch to disk.}
\item{retain_draws}{Logical; retain posterior draws in memory.}
}}

\item{LGFM}{Logical. If \code{TRUE}, runs the LGFM model (GeoMix without Z1 data model)
instead of GeoMix. Default is \code{FALSE}.}

\item{run_parallel}{Logical. If \code{TRUE}, chains are run in parallel using
\pkg{future} with multisession workers. Default is \code{FALSE}.}

\item{load_previous_state}{Logical. If \code{TRUE}, attempts to resume each chain
from previously saved batch files in \code{path}. Requires
\code{save_batches = TRUE}. Default is \code{FALSE}.}

\item{mc.cores}{Optional integer giving the number of parallel workers when
\code{run_parallel = TRUE}. Defaults to the minimum of \code{nchains} and available
cores.}

\item{seed}{Integer random seed used to initialise chains. Chain \code{j} uses
seed \code{seed + j}.}
}
\value{
If \code{retain_draws = TRUE}, returns a list with components:
\describe{
\item{chains}{Per-chain metadata and results.}
\item{samples}{Named list of posterior draw matrices, one per chain.}
}

Otherwise returns only the per-chain result list.

Each chain result contains:
\describe{
\item{chain}{Chain index.}
\item{name}{Chain name \code{GeoMix_j} (or \code{LGFM_j} when \code{LGFM == TRUE}).}
\item{path}{Output directory used for saved batches.}
\item{batches_run}{Batch indices completed.}
\item{batch_sizes}{Iterations per batch.}
\item{controlMCMC}{Resolved MCMC control list.}
\item{samples}{Posterior draws matrix, if retained.}
}
}
\description{
Runs one or more Markov chain Monte Carlo (MCMC) chains for a fitted
GeoMix model object. Chains may be run sequentially or in parallel,
with optional batch-wise saving to disk and
optional continuation from a previously saved state.
}
\details{
The function constructs the NIMBLE model, configures custom samplers,
compiles the model and MCMC objects, and executes the requested number
of iterations split across batches.

Iterations are divided into approximately equal batch sizes using
\code{nbatches}. After each batch:
\itemize{
\item posterior draws may be saved to disk;
\item draws may be retained in memory;
\item in-memory sample storage is cleared to reduce RAM usage.
}

When \code{load_previous_state = TRUE}, the function loads the most recent saved
batch for each chain, reconstructs parameter initial values, and continues
sampling from that state.

Parallel execution uses \code{future::plan(multisession)}.
}
\examples{
\dontrun{
# Run a single chain
fit <- run_chains(geomix_setup)

# Run four chains in parallel
fit <- run_chains(
  geomix_setup,
  nchains = 4,
  run_parallel = TRUE
)

# Save batches to disk
fit <- run_chains(
  geomix_setup,
  nchains = 2,
  path = "results/mcmc",
  controlMCMC = list(
    niter = 5000,
    nbatches = 5,
    save_batches = TRUE
  )
)
}

}
\seealso{
\code{\link[nimble:nimble-package]{nimble}},
\code{\link[future:plan]{future::plan}},
\code{\link[future.apply:future_lapply]{future.apply::future_lapply}}
}

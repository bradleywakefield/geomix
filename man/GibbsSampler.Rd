% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nimble_zGibbsSampler.R
\name{GibbsSampler}
\alias{GibbsSampler}
\title{Gibbs sampler for latent class labels with confusion and GP updates}
\usage{
GibbsSampler(model, mvSaved, target, control)
}
\arguments{
\item{model}{Compiled or uncompiled NIMBLE model object.}

\item{mvSaved}{NIMBLE modelValues object.}

\item{target}{Character string giving the sampler target node.}

\item{control}{Named list of sampler constants and indexing structures.}
}
\value{
No return value. Updates \code{model$Y1} in place.
}
\description{
Internal NIMBLE sampler that updates the latent class field \code{Y1} using
Gibbs steps combining Potts prior contributions, boundary-aware confusion
likelihood terms for \code{Z1}, and grouped Gaussian process likelihood changes
for \code{Z2}.
}
\details{
For each site, the sampler constructs the local posterior over class
assignments using three components:
\itemize{
\item the weighted Potts prior from neighbouring labels;
\item the boundary-aware confusion contribution from observed labels \code{Z1};
\item the grouped Gaussian process likelihood contribution from \code{Z2},
when observed at the site.
}

A local boundary window is determined from the current value of \code{h}, and
grouped Gaussian process log-density terms are updated incrementally for
affected groups only.
}
\keyword{internal}

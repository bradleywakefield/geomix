% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateCovariance.R
\name{estimate_MAP_covariance}
\alias{estimate_MAP_covariance}
\title{Estimate MAP covariance parameters for the grouped GP model}
\usage{
estimate_MAP_covariance(geomix_setup)
}
\arguments{
\item{geomix_setup}{A GeoMix setup object containing model constants, data,
and initial values. It must contain at least:
\describe{
\item{\code{constants}}{A list containing all constants required by the
\code{dGPgroupvec_margAlpha} distribution, including \code{K}, \code{N1}, \code{N2}, \code{p},
\code{D}, \code{L}, \code{G}, \code{m}, \code{mG}, indexing objects, distance matrices, and
prior hyperparameters.}
\item{\code{data_list}}{A list containing \code{Z1}, \code{Z2}, \code{X}, \code{Z2_ind},
\code{dID}, \code{locID}, and any other required model data.}
\item{\code{inits}}{A list containing initial values for \code{sigma2}, \code{tau2},
\code{sigma2_L}, \code{sigma2_D}, \code{lL}, and \code{lD}.}
}}
}
\value{
A named list containing MAP estimates of the covariance parameters:
\describe{
\item{lL}{Numeric vector of length \code{K} containing the estimated lateral
length-scale parameters.}
\item{lD}{Numeric vector of length \code{K} containing the estimated depth
length-scale parameters.}
\item{sigma2}{Numeric vector of length \code{K} containing the estimated
class-specific process variance parameters.}
\item{tau2}{Numeric scalar giving the estimated nugget or noise variance.}
}
}
\description{
Estimates covariance parameters for the GeoMix grouped Gaussian process
component by maximising the marginal posterior density of the observed
geotechnical response \code{Z2}, with regression coefficients integrated out.
}
\details{
The optimisation is carried out on the log scale for positivity-constrained
parameters, using \code{optim()}. The latent class field \code{Y1} is fixed to the
observed labels \code{Z1}, and the function returns the resulting MAP estimates
for the covariance parameters.

The model fitted by this function includes priors for
\eqn{\tau^2}, \eqn{\sigma_L^2}, \eqn{\sigma_D^2}, and the class-specific
covariance parameters \eqn{l_{L,k}}, \eqn{l_{D,k}}, and \eqn{\sigma_k^2}.
The likelihood for \code{Z2} is defined through the custom
\code{dGPgroupvec_margAlpha} distribution, which integrates out the regression
coefficients \code{alpha}.

Before optimisation:
\itemize{
\item \code{Y1} is fixed to \code{Z1}
\item only the covariance-related initial values are retained
\item the model is compiled with \pkg{nimble}
}

Optimisation is performed over the transformed parameter vector
\deqn{
(\log(lL_1 + 1), \ldots, \log(lL_K + 1),
  \log(lD_1 + 1), \ldots, \log(lD_K + 1),
  \log(\sigma^2_1), \ldots, \log(\sigma^2_K),
  \log(\tau^2)),
}
and the resulting optimiser output is back-transformed before being
returned.

The objective function is the negative log-posterior contribution from the
\code{Z2} node only, evaluated using \code{Cmodel$calculate("Z2")}. If the evaluated
log-density is non-finite, a large penalty value is returned to stabilise
optimisation.

Note that this function does not return the full \code{optim()} object, Hessian,
or convergence diagnostics. It only returns the transformed MAP estimates.
}
\examples{
\dontrun{
data(offshore)

setup <- setupGeoMixModel(
  data      = offshore,
  K         = 3,
  dims      = c(20, 20, 20),
  variables = list(
    loc = "locID", xID = "x", yID = "y", dID = "d",
    x = "x", y = "y", depth = "d", Z1 = "Z1", Z2 = "Z2"
  ),
  aformula  = ~ d,
  m         = 10
)

# Estimate MAP covariance parameters (uses observed Z1 as fixed class labels)
map_cov <- estimate_MAP_covariance(setup)
map_cov$sigma2   # class-specific process variances
map_cov$tau2     # noise variance
map_cov$lL       # lateral length scales
map_cov$lD       # depth length scales

# Use MAP estimates as initial values for MCMC to improve mixing
setup2 <- setupGeoMixModel(
  data      = offshore,
  K         = 3,
  dims      = c(20, 20, 20),
  variables = list(
    loc = "locID", xID = "x", yID = "y", dID = "d",
    x = "x", y = "y", depth = "d", Z1 = "Z1", Z2 = "Z2"
  ),
  aformula  = ~ d,
  m         = 10,
  inits     = map_cov
)
}

}
\seealso{
\code{\link[=setupGeoMixModel]{setupGeoMixModel()}}, \code{\link[=run_chains]{run_chains()}}, \code{\link[stats:optim]{stats::optim()}}
}
